# دليل شامل لنظام الصلاحيات والمخازن

## 🎯 كيف يعمل النظام

### المفهوم الأساسي:
1. **المستخدم** (User) - له دور (Role)
2. **المخزن** (Warehouse) - مكان تخزين المنتجات
3. **الصلاحية** (Permission) - ربط بين المستخدم والمخزن

```
المستخدم + المخزن = الصلاحية
```

---

## 📋 خطوات إنشاء مستخدم جديد وإعطائه صلاحيات

### الخطوة 1: إنشاء المستخدم

#### من واجهة الموقع:
1. اذهب إلى **"إدارة المستخدمين"** (يجب أن تكون Admin)
2. اضغط **"إضافة مستخدم جديد"**
3. املأ البيانات:
   ```
   الاسم: أحمد محمد
   البريد الإلكتروني: ahmed@company.com
   كلمة المرور: ********
   الدور: مدير المخزون (inventory_manager)
   الحالة: نشط ✓
   ```
4. اضغط **"إضافة المستخدم"**

#### من قاعدة البيانات (SQL):
```sql
-- إنشاء مستخدم جديد
INSERT INTO users (
  name,
  email,
  password_hash,
  role,
  is_active
)
VALUES (
  'أحمد محمد',
  'ahmed@company.com',
  '$2a$10$...', -- كلمة المرور المشفرة
  'inventory_manager',
  true
);
```

---

### الخطوة 2: منح صلاحيات المخزن

#### من واجهة الموقع:
1. اذهب إلى **"صلاحيات المخازن"**
2. اختر تبويب **"منح صلاحية جديدة"**
3. املأ البيانات:
   ```
   المستخدم: أحمد محمد (ahmed@company.com)
   المخازن: ☑ مخزن القاهرة
   مستوى الصلاحية: مدير (admin)
   
   الصلاحيات التفصيلية:
   ☑ عرض البيانات
   ☑ إضافة عناصر
   ☑ تعديل البيانات
   ☑ حذف البيانات
   ```
4. اضغط **"منح الصلاحية"**

#### من قاعدة البيانات (SQL):
```sql
-- الحصول على معرف المستخدم
SELECT id FROM users WHERE email = 'ahmed@company.com';
-- النتيجة: 'user-uuid-123'

-- الحصول على معرف المخزن
SELECT id FROM warehouses WHERE name = 'مخزن القاهرة';
-- النتيجة: 1

-- منح الصلاحية
INSERT INTO user_warehouse_permissions (
  user_id,
  warehouse_id,
  permission_level,
  can_view,
  can_add,
  can_edit,
  can_delete
)
VALUES (
  'user-uuid-123',
  1,
  'admin',
  true,
  true,
  true,
  true
);
```

---

### الخطوة 3: التحقق من الصلاحيات

#### من واجهة الموقع:
1. اذهب إلى **"صلاحيات المخازن"**
2. ابحث عن المستخدم في تبويب **"الصلاحيات الحالية"**
3. تحقق من ظهور المخزن والصلاحيات

#### من قاعدة البيانات (SQL):
```sql
-- التحقق من صلاحيات المستخدم
SELECT 
  u.name as user_name,
  u.email,
  u.role,
  w.name as warehouse_name,
  uwp.permission_level,
  uwp.can_view,
  uwp.can_add,
  uwp.can_edit,
  uwp.can_delete
FROM user_warehouse_permissions uwp
JOIN users u ON uwp.user_id = u.id
JOIN warehouses w ON uwp.warehouse_id = w.id
WHERE u.email = 'ahmed@company.com';
```

**النتيجة المتوقعة**:
```
user_name: أحمد محمد
email: ahmed@company.com
role: inventory_manager
warehouse_name: مخزن القاهرة
permission_level: admin
can_view: true
can_add: true
can_edit: true
can_delete: true
```

---

## 🔍 ماذا يرى المستخدم بعد تسجيل الدخول؟

### مستخدم بدور `inventory_manager` مع صلاحيات على مخزن واحد:

#### القائمة الجانبية:
```
✅ لوحة التحكم
✅ إدارة المخزون
✅ المخازن (عرض فقط)
✅ الفئات
✅ الإصدارات
✅ البحث المتقدم
```

#### لوحة التحكم:
```
إجمالي المخزون: XX (من مخزن القاهرة فقط)
المنتجات المتوفرة: XX (من مخزن القاهرة فقط)
الإصدارات اليوم: XX (من مخزن القاهرة فقط)
إجمالي المنتجات: XX (في مخزن القاهرة فقط)
```

#### صفحة المخازن:
```
المخازن المعروضة:
- مخزن القاهرة (فقط)

الإجراءات المتاحة:
- عرض فقط ✓
- لا يمكن إضافة مخزن ✗
- لا يمكن تعديل المخزن ✗
- لا يمكن حذف المخزن ✗
```

#### صفحة إدارة المخزون:
```
المنتجات المعروضة:
- منتجات مخزن القاهرة فقط

الإجراءات المتاحة:
- إضافة منتج ✓
- تعديل منتج ✓
- حذف منتج ✓ (حسب الصلاحيات)
```

---

## 🎭 الأدوار والصلاحيات

### 1. Admin (مدير النظام)
```
الدور في قاعدة البيانات: admin

الصلاحيات:
✅ رؤية جميع المخازن
✅ إدارة جميع المنتجات
✅ إضافة/تعديل/حذف المخازن
✅ إدارة المستخدمين
✅ إدارة صلاحيات المخازن
✅ إدارة العملاء والفروع
✅ جميع التقارير

لا يحتاج إلى صلاحيات مخازن - له صلاحيات كاملة تلقائياً
```

### 2. Inventory Manager (مدير المخزون)
```
الدور في قاعدة البيانات: inventory_manager

الصلاحيات الأساسية:
✅ رؤية المخازن المسموح له بها فقط
✅ إدارة منتجات مخازنه
✅ إصدار منتجات من مخازنه
✅ البحث في بيانات مخازنه

يحتاج إلى صلاحيات مخازن محددة:
- يجب إضافته في "صلاحيات المخازن"
- يمكن إعطاؤه صلاحيات على مخزن واحد أو أكثر
- الصلاحيات تحدد ما يمكنه فعله (عرض/إضافة/تعديل/حذف)
```

### 3. Engineer (مهندس)
```
الدور في قاعدة البيانات: engineer

الصلاحيات:
✅ لوحة التحكم (عرض عام)
✅ الإصدارات
✅ البحث المتقدم
❌ لا يرى إدارة المخزون
❌ لا يرى المخازن
❌ لا يرى الفئات

لا يحتاج إلى صلاحيات مخازن
```

---

## 🔐 مستويات الصلاحيات

### View (عرض فقط)
```
permission_level: view

الصلاحيات:
✅ can_view: true
❌ can_add: false
❌ can_edit: false
❌ can_delete: false

ماذا يمكنه فعله:
- رؤية المنتجات
- رؤية الإصدارات
- البحث
- لا يمكنه التعديل أو الإضافة
```

### Edit (تعديل)
```
permission_level: edit

الصلاحيات:
✅ can_view: true
✅ can_add: true
✅ can_edit: true
❌ can_delete: false

ماذا يمكنه فعله:
- رؤية المنتجات
- إضافة منتجات جديدة
- تعديل المنتجات الموجودة
- إصدار منتجات
- لا يمكنه حذف المنتجات أو المخازن
```

### Admin (مدير)
```
permission_level: admin

الصلاحيات:
✅ can_view: true
✅ can_add: true
✅ can_edit: true
✅ can_delete: true

ماذا يمكنه فعله:
- جميع صلاحيات Edit
- حذف المنتجات
- إدارة كاملة للمخزن
- لا يمكنه حذف المخزن نفسه (فقط Admin النظام)
```

---

## 📝 سيناريوهات عملية

### سيناريو 1: مدير مخزن واحد

**الإعداد**:
```sql
-- المستخدم
name: أحمد محمد
email: ahmed@company.com
role: inventory_manager

-- الصلاحية
warehouse: مخزن القاهرة
permission_level: admin
can_view: true
can_add: true
can_edit: true
can_delete: true
```

**ماذا يرى**:
- لوحة التحكم: إحصائيات مخزن القاهرة فقط
- المخازن: مخزن القاهرة فقط (عرض)
- المنتجات: منتجات مخزن القاهرة فقط
- الإصدارات: يمكنه الإصدار من مخزن القاهرة

---

### سيناريو 2: مدير عدة مخازن

**الإعداد**:
```sql
-- المستخدم
name: محمد علي
email: mohamed@company.com
role: inventory_manager

-- الصلاحيات
warehouse 1: مخزن القاهرة (admin)
warehouse 2: مخزن الإسكندرية (edit)
warehouse 3: مخزن الجيزة (view)
```

**ماذا يرى**:
- لوحة التحكم: إحصائيات المخازن الثلاثة مجتمعة
- المخازن: المخازن الثلاثة فقط
- المنتجات: منتجات المخازن الثلاثة
- الإجراءات:
  - مخزن القاهرة: إضافة/تعديل/حذف ✓
  - مخزن الإسكندرية: إضافة/تعديل ✓، حذف ✗
  - مخزن الجيزة: عرض فقط

---

### سيناريو 3: موظف بدون صلاحيات

**الإعداد**:
```sql
-- المستخدم
name: سارة أحمد
email: sara@company.com
role: inventory_manager

-- الصلاحيات
لا توجد صلاحيات مخازن
```

**ماذا يرى**:
- لوحة التحكم: إحصائيات فارغة (0)
- المخازن: لا توجد مخازن
- المنتجات: لا توجد منتجات
- رسالة: "لا توجد بيانات متاحة"

**الحل**: إضافة صلاحيات مخزن من صفحة "صلاحيات المخازن"

---

## 🛠️ استكشاف الأخطاء

### المشكلة 1: المستخدم لا يرى "إدارة المخزون"

**السبب المحتمل**:
- الدور خاطئ (engineer بدلاً من inventory_manager)

**الحل**:
```sql
-- تحديث الدور
UPDATE users 
SET role = 'inventory_manager'
WHERE email = 'user@email.com';
```

---

### المشكلة 2: المستخدم يرى جميع المخازن

**السبب المحتمل**:
- الدور admin بدلاً من inventory_manager

**التحقق**:
```sql
SELECT role FROM users WHERE email = 'user@email.com';
```

**الحل**:
- إذا كان admin: هذا صحيح، Admin يرى كل شيء
- إذا كان inventory_manager: تحقق من الكود

---

### المشكلة 3: لوحة التحكم تعرض بيانات جميع المخازن

**السبب المحتمل**:
- الكود لا يمرر userId للدوال

**التحقق**:
```typescript
// في app/dashboard/page.tsx
const userId = user.role !== 'admin' ? user.id : undefined
getDashboardStats(userId) // يجب تمرير userId
```

---

### المشكلة 4: لا توجد صلاحيات للمستخدم

**السبب المحتمل**:
- لم يتم إضافة المستخدم في "صلاحيات المخازن"

**الحل**:
1. اذهب إلى "صلاحيات المخازن"
2. اختر "منح صلاحية جديدة"
3. اختر المستخدم والمخزن
4. حدد الصلاحيات
5. اضغط "منح الصلاحية"

---

## 📊 جدول مقارنة الأدوار

| الميزة | Admin | Inventory Manager | Engineer |
|--------|-------|-------------------|----------|
| رؤية جميع المخازن | ✅ | ❌ (مخازنه فقط) | ❌ |
| إدارة المخزون | ✅ | ✅ (مخازنه) | ❌ |
| إضافة/حذف مخازن | ✅ | ❌ | ❌ |
| الإصدارات | ✅ | ✅ | ✅ |
| إدارة المستخدمين | ✅ | ❌ | ❌ |
| صلاحيات المخازن | ✅ | ❌ | ❌ |
| التقارير | ✅ | محدودة | ❌ |

---

## ✅ قائمة التحقق

عند إنشاء مستخدم جديد:

- [ ] إنشاء المستخدم في "إدارة المستخدمين"
- [ ] تحديد الدور الصحيح (inventory_manager)
- [ ] تفعيل الحساب (is_active = true)
- [ ] الذهاب إلى "صلاحيات المخازن"
- [ ] منح صلاحيات المخزن المطلوب
- [ ] تحديد مستوى الصلاحية (view/edit/admin)
- [ ] تحديد الصلاحيات التفصيلية
- [ ] التحقق من الصلاحيات في تبويب "الصلاحيات الحالية"
- [ ] تسجيل الدخول بالحساب الجديد
- [ ] التحقق من القائمة الجانبية
- [ ] التحقق من لوحة التحكم
- [ ] التحقق من صفحة المخازن

---

## 🎓 ملخص سريع

### لإنشاء مدير مخزن:
1. **إدارة المستخدمين** → إضافة مستخدم → الدور: `inventory_manager`
2. **صلاحيات المخازن** → منح صلاحية → اختر المستخدم والمخزن
3. **تسجيل الدخول** → تحقق من الصلاحيات

### ما يجب أن يراه:
- ✅ لوحة التحكم (مخزنه فقط)
- ✅ إدارة المخزون
- ✅ المخازن (عرض فقط)
- ✅ الإصدارات

### ما لا يجب أن يراه:
- ❌ جميع المخازن
- ❌ أزرار حذف المخازن
- ❌ إدارة المستخدمين
- ❌ صلاحيات المخازن

---

## 📞 الدعم

إذا لم يعمل شيء:
1. تحقق من الدور في قاعدة البيانات
2. تحقق من صلاحيات المخزن
3. امسح الـ cache
4. سجل الدخول مرة أخرى
5. راجع ملف `FINAL_SUMMARY.md`