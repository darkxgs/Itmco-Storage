# ترحيل الأمان: تسجيل الدخول عبر Supabase Auth وقفل قاعدة البيانات

## ليه؟

قبل التحديث ده، أي حد يفتح الموقع كان يقدر ياخد المفتاح العام (anon key) من الصفحة ويستخدمه في:

- قراءة كل المستخدمين ومعاهم `password_hash`
- تعديل دوره لـ `admin`، أو تعديل وحذف أي منتج أو إصدار
- تحميل نسخة كاملة من قاعدة البيانات من `/api/backup`، أو إدخال بيانات عن طريق `/api/restore`، بدون تسجيل دخول

وكمان المخزون كان بيتحسب في المتصفح على خطوات منفصلة، فلو اتنين صرفوا نفس الصنف في نفس الوقت الرصيد كان بيبوظ.

بعد التحديث:

- تسجيل الدخول بقى بـ **Supabase Auth**.
- كل الجداول محمية بـ **RLS** حسب الدور.
- الصرف والتعديل والحذف وإضافة الكميات بقت عمليات واحدة جوه قاعدة البيانات (transaction)، فيا تتم كلها يا ماتتمش خالص.

**حسابات الموظفين زي ما هي بالظبط:** نفس الإيميل ونفس كلمة المرور. السكريبت بينقل كلمة المرور المشفرة زي ما هي، ومحدش هيحتاج يغيّر حاجة.

## الترتيب مهم: اعمل الخطوات بالترتيب ده

> لو شغّلت ملف القفل (الخطوة 6) قبل نشر النسخة الجديدة (الخطوة 5)، الموقع الحالي هيتقفل عن قاعدة البيانات.

### 1. هات مفتاح service role

من Supabase: Project Settings ← API ← `service_role` (secret).

- المفتاح ده سري جداً، ماينفعش يتحط في متغير يبدأ بـ `NEXT_PUBLIC_`.
- ماينفعش يترفع على GitHub.

### 2. انقل المستخدمين لـ Supabase Auth

محلياً في فولدر المشروع، اعمل ملف `.env.local` (موجود في `.gitignore` وماينفعش يترفع) فيه:

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

بعدين شغّل:

```bash
node scripts/migrate-users-to-supabase-auth.mjs --dry-run
node scripts/migrate-users-to-supabase-auth.mjs
```

- السكريبت آمن لو اتشغل أكتر من مرة.
- أي سطر مكتوب فيه `ATTENTION` محتاج تتصرف فيه يدوياً، زي مستخدم من غير كلمة مرور أو إيميل متكرر.

### 3. اقفل التسجيل العام

من Supabase: Authentication ← Sign In / Providers ← Email ← اقفل **Allow new users to sign up**.

المستخدمين الجداد بيتعملوا من صفحة "إدارة المستخدمين" بس.

### 4. شغّل ملف دوال المخزون

في Supabase ← SQL Editor، شغّل الملف ده كله:

`supabase/migrations/20260923110000_stock_functions.sql`

الملف ده **بيضيف بس** ومش بيقفل حاجة، فالنسخة الحالية من الموقع هتفضل شغالة عادي بعده. هو اللي بيعمل:

- دوال الصرف والتعديل والحذف وإضافة الكمية.
- عمودين `unit_price` و `unit_cost` لسعر الصرف وقت الإصدار، وبينقل أسعار البيع القديمة من الملاحظات للعمود الجديد.

### 5. انشر النسخة الجديدة

1. في Vercel ← Settings ← Environment Variables، ضيف:
   - `SUPABASE_SERVICE_ROLE_KEY`: في Production و Preview.
   - `CRON_SECRET`: قيمة عشوائية طويلة، وهي اللي بتشغّل النسخ الاحتياطي التلقائي اليومي.
2. اعمل merge للـ branch واستنى الـ deploy يخلص.
3. جرّب تسجيل الدخول بحساب لكل دور.

### 6. شغّل ملف القفل

في Supabase ← SQL Editor، شغّل الملف ده كله:

`supabase/migrations/20260923120000_auth_rls_lockdown.sql`

لو اتشغّل قبل الخطوة 4، هيرفض ويقولك تشغّل ملف الدوال الأول.

### 7. اختبر

- **مهندس:** يصرف منتجين مع بعض، ويعدّل الكمية، ويحذف إصدار عمله هو.
- **مدير مخزون:** يضيف منتج، ويضيف كمية، ويعدّل رصيد منتج، ويطلع تقرير Excel.
- **مدير النظام:** يضيف مستخدم، ويغيّر كلمة مرور، ويعمل نسخة احتياطية.
- **من غير تسجيل دخول:** الطلب ده لازم يرجع permission denied:

  ```bash
  curl "https://xxxx.supabase.co/rest/v1/users?select=*" -H "apikey: <ANON_KEY>"
  ```

### 8. اختياري: إصلاح النصوص المشفرة

أي اسم كان فيه `/` أو `'` أو `&` ممكن يكون اتحفظ مشفّر، زي `&#x2F;` بدل `/`.

- شغّل `scripts/repair-escaped-text.sql` زي ما هو: بيعرض عدد القيم اللي محتاجة إصلاح بس، ومش بيغيّر حاجة.
- لو الأعداد مظبوطة، غيّر `apply` لـ `true` وشغّله تاني.

## ملاحظة عن كلمات المرور

كلمات مرور الحسابات الثلاثة الأساسية كانت مكتوبة في README، والمستودع على GitHub **عام**، فأي حد شاف المستودع يعرفها.

- اتشالت من README، ومحفوظة عندك في `accounts.local.md` على جهازك بس (مش بيترفع).
- تغييرها قرارك: لو عايز تفضل زي ما هي عادي، ولو حبيت تغيّرها بعدين هتلاقيها في صفحة "إدارة المستخدمين".
- الأأمن إنك تخلي المستودع **private** من GitHub ← Settings.

بعد ما الكل يسجّل دخول بنجاح، تقدر تمسح النسخة القديمة من كلمات المرور المشفرة (اختياري):

```sql
update public.users set password_hash = null;
```

## لو حاجة وقفت بعد الخطوة 6

- رسالة زي `permission denied` أو `row-level security` معناها إن فيه دور محتاج صلاحية مش موجودة في السياسات.
- ماترجعش السياسات القديمة `USING (true)`، لأن ده بيرجّع الثغرة كلها.
- بدل كده: حدّد العملية والدور، وضيف السياسة الناقصة.
- وماتشغّلش سكريبتات `scripts/` القديمة على قاعدة البيانات الحقيقية؛ عليها تحذير في أولها.
